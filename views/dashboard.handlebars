<div class="nav-wrap">
    <nav>
      <ul>
        <li><a href="/">Home</a> </li>
        <li>Dashboard</li>
        {{#if loggedIn}}
        <li><button id="logout" class="hide-btn">Logout</button></li>
        {{else}}
        <li><a href="/login">Login</a></li>
        {{/if}}
      </ul>
      <h1>Your Dashboard</h1>
    </nav>
  </div>